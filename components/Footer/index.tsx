import { Layout } from 'antd';
const { Footer } = Layout;

const index = () => {
  return (
    <Footer style={{ textAlign: 'center' }}>OrbitRemit ©2021 Created by OrbitRemit</Footer>
  );
}

export default index;
